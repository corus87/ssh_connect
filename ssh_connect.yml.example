# Optional settings file, expected at ~/.ssh/ssh_connect.yml
# Hosts are NOT configured here, they live in ~/.ssh/ssh_connect.conf.

theme: material

hosts:
  appliance:
    password: secret        # login via sshpass
  legacy-box:
    skip_key_setup: true    # never offer to upload a public key
